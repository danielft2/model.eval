export type ImportedQuestion = {
  id: string;
  descriptorCode: string;
  title: string;
  text: string;
  command: string;
  descriptor: string;
  answer: string;
  answerItem: string;
  options: string;
}